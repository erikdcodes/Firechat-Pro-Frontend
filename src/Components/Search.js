import styled from "styled-components";

const Search = () => {
  return (
    <Wrapper>
      <input type="text" />
    </Wrapper>
  );
};

const Wrapper = styled.div`
  padding: 0 30px;
`;

export default Search;
